<UserControl x:Class="Erronka.Views.TicketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🧾 Tabla del ticket -->
        <DataGrid x:Name="OrderItemsGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Grid.Row="0"
                  HeadersVisibility="Column"
                  FontSize="14"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Artículo" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Cant" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Importe" Binding="{Binding Total}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- ⚙️ Botones de control del ticket -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="🆕 Nuevo Ticket" Width="120" Margin="5"/>
            <Button Content="✏️ Editar Línea" Width="120" Margin="5"/>
            <Button Content="❌ Eliminar Línea" Width="120" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>





