<UserControl x:Class="Erronka.Views.TicketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="OrderItemsGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Grid.Row="0"
                  HeadersVisibility="Column"
                  FontSize="14"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Artikuloa" Binding="{Binding Izena}" Width="*"/>
                <DataGridTextColumn Header="Prezioa" Binding="{Binding Prezioa}" Width="100"/>
                <DataGridTextColumn Header="kantitatea" Binding="{Binding Kantitatea}" Width="80"/>
                <DataGridTextColumn Header="Zenbatekoa" Binding="{Binding Totala}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="🆕 Ticket Berria" Width="120" Margin="5" Click="TicketBerria_Click"/>
            <Button Content="✏️ Aldatu" Width="120" Margin="5"/>
            <Button Content="❌ Ezeztatu" Width="120" Margin="5" Click="EzabatuItem"/>
        </StackPanel>
    </Grid>
</UserControl>





